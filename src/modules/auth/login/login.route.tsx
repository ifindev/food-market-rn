import {AppAuthRoute} from '@typings/navigation.type';
import LoginView from './login.view';

const loginRoute: AppAuthRoute = {
  component: LoginView,
  name: 'Login',
};

export default loginRoute;

import {AppAuthRoute} from '@typings/navigation.type';
import RegisterView from './register.view';

const registerRoute: AppAuthRoute = {
  component: RegisterView,
  name: 'Register',
};

export default registerRoute;

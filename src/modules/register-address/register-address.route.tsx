import {AppAuthRoute} from '@typings/navigation.type';
import RegisterAddressView from './register-address.view';

const registerAddressRoute: AppAuthRoute = {
  component: RegisterAddressView,
  name: 'RegisterAddress',
};

export default registerAddressRoute;
